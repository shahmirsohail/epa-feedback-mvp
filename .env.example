# Database
DATABASE_URL="file:./dev.db"

# OpenAI
OPENAI_API_KEY=""
# Speech-to-text model for /api/transcribe
OPENAI_TRANSCRIBE_MODEL="gpt-4o-mini-transcribe"
# Chat model for EPA classification + feedback drafting
OPENAI_CHAT_MODEL="gpt-4o-mini"

# Email (SMTP)
# For local testing, run Mailpit and use host=127.0.0.1 port=1025 secure=false.
# Production example: smtp.gmail.com (port 587) or your org SMTP relay.
SMTP_HOST="127.0.0.1"
SMTP_PORT="587"
SMTP_SECURE="false" # true for 465
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="epa-drafts@localhost"

# App
APP_BASE_URL="http://localhost:3000"
